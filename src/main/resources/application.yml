server:
  port: 8089

#配置数据源
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/depts?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis的配置
mybatis:
  mapper-locations: classpath:mapping/*UserMapper.xml
  type-aliases-package: com.example.demo.entity
